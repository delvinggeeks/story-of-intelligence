# Evidence Register

| ID | Date | Method | Result | Related item | Status |
| --- | --- | --- | --- | --- | --- |
| E-001 | 2026-08-02 | Ran `node scripts/validate-learning-object.mjs schemas/learning-object.schema.v1.json content/learning-objects/numbers.v1.json`. | The `Numbers` object passed required-field, identity, learning-flow, measurement, and tutor-guidance checks. | A-001 | Supports, but does not yet fully validate learner outcomes. |
| E-002 | 2026-08-02 | Ran `npm run verify` (`node --check assets/app.js` + LOS validation + Playwright E2E). | All checks passed. Playwright verified full pre/lesson/post flow, hint gating, tutor interaction, and completion save response. | A-001 | Supports technical readiness of one LOS-driven vertical slice. |
| E-003 | 2026-08-02 | Browser automation persisted completion via `/api/progress` through Finish action and observed completion state in UI. | Runtime accepted completion payload and the app locked final state as expected. | A-004 | Supports that adaptive tutor + assessment loop is operational; learning-effectiveness still requires real learner outcome study. |
| E-004 | 2026-08-02 | Ran `npm run test:ci` after setting `content/learning-objects/numbers.v1.json` provenance status to `validated`. | `validate`, `test:unit`, and `test:e2e` all passed with zero failures (Node 5/5, Playwright 5/5). | A-001, A-003 | Supports that the Numbers lesson artifact is spec-conformant and test-verified for owner review. |
| E-005 | 2026-08-02 | Implemented `dashboard.html` + `/api/dashboard`, then ran `npm run test:unit` and `npm run test:e2e`. | Dashboard endpoint test passed (Node 6/6) and dashboard UI journey passed in Playwright (E2E 6/6). | A-001, A-003 | Supports full-stack learner dashboard readiness without expanding beyond Numbers slice. |
| E-006 | 2026-08-02 | Added production server hardening, Docker/Compose artifacts, health/readiness and lesson-contract tests; ran `npm run test:ci` and workspace diagnostics. | LOS validation passed; Node 10/10 and Playwright 7/7 passed; no diagnostics. Docker build/smoke test is configured in GitHub Actions but could not run locally because Docker is not installed. | A-001, A-003 | Supports production definition and complete Numbers lesson contract; local container execution remains an external prerequisite. |
| E-007 | 2026-08-02 | Located Docker Desktop 4.84.0, ran Docker CLI 29.6.2 and Compose 5.3.1 directly, and executed `docker compose config --quiet`. | CLI and Compose are installed; Compose configuration passed. Docker engine cannot start because Windows Subsystem for Linux is not installed. | A-001 | Container definition is structurally validated; image build/run awaits WSL installation and restart. |
| E-008 | 2026-08-02 | Verified WSL 2 and Docker engine 29.6.2, ran `docker compose config --quiet`, `docker compose build`, and `docker compose up -d`; probed health, readiness, and dashboard endpoints; inspected runtime identity and logs. | Image built successfully; container reached `healthy`; `/healthz`, `/readyz`, and `/dashboard.html` returned HTTP 200; service runs as UID 1000 (`node`); startup logs were clean. | A-001, A-003 | Validates the production container and local Compose run path for the complete Numbers vertical slice. |
| E-009 | 2026-08-02 | Implemented ADR-0002; ran `npm run test:ci`, rebuilt with `docker compose up --build -d`, probed graph/dashboard/final-lesson APIs, and inspected the dashboard at desktop and 390 px browser widths. | Curriculum validation passed for 11 graph nodes and 11 LOS objects; Node 12/12 and Playwright 8/8 passed; container reached `healthy` as UID 1000; deployed APIs returned 11 lessons ending in Linear Regression From Scratch; mobile had no horizontal overflow. | ADR-0002, A-001, A-005 | Validates structural and runtime completion of the staged curriculum; beginner learning effectiveness and content depth remain open. |
| E-010 | 2026-08-02 | Deepened the ten ADR-0002 lessons to two full observe→wonder→predict→explain→apply depth loops (10 steps each; loop 1 daily-life lens, loop 2 data/engineering/ML lens, final apply bridging to the next lesson); added an all-lesson depth contract test; reran `npm run test:ci`; rebuilt the container and probed `/healthz` plus two deepened lesson APIs. | Curriculum validation passed (11/11); Node 13/13 including the new "two depth loops and bridges forward" gate for every lesson; Playwright 8/8 including the 10-step final-lesson journey; container `healthy` and serving 10-step lessons with a final `apply` step. | A-005, R-037, R-038 | Validates structural depth and flow-bridging of all 11 lessons; pedagogical sufficiency of two loops still requires owner eye-test (A-005 remains open). |
| E-011 | 2026-08-02 | Implemented ADR-0003 (LOS v2.0: mental models, analogies, history, stability, nextConcept, mastery rubric, 16-kind step enum with Experiment→Fail→Discover arc), ADR-0004 (zero-dependency interactive experiment engine, 11 playable experiments — one per lesson), and ADR-0005 (rubric-reasoning tutor + server-side mastery-gated completion); migrated all 11 Learning Objects to v2 (13 steps each), archived v1, repointed the graph; ran `npm run validate`, `npm run test:unit`, `npm run test:e2e`; rebuilt the container and probed `/healthz` and two v2 lesson APIs. | Curriculum validation passed (11 nodes / 11 v2 objects); Node 14/14 including new knowledge-asset/experiment/rubric gates; Playwright 10/10 including a live experiment interaction (unit-compare flip) and a mastery-gate refusal path; container `healthy`, serving version 2.0.0 lessons with experiment specs. | ADR-0003, ADR-0004, ADR-0005, A-006..A-010 | Validates that the platform now runs playable experiments, evidence-scored gating, and richer Learning Objects end to end; learner-outcome validation remains open. |
| E-012 | 2026-08-03 | Under ADR-0006 Phase A/B remediation: made content-root/`.env` resolution working-directory independent (`core/paths.py`), moved the step-label taxonomy from the frontend into the API contract (`STEP_KIND_LABELS`, serialised as `steps[].label`), disabled Next.js telemetry via `apps/web/scripts/next.mjs`, pinned `.python-version` and `[tool.uv] link-mode = "copy"`, made `typecheck:web` clean-checkout safe, and made the `npm audit` CI job advisory rather than a build gate. Ran `uv lock --check`, `pytest`, `ruff check`, `ruff format --check`, `mypy`, `npm run lint:web`, `npm run test:web`, `npm run build:web`, `npm run typecheck:web`, and `next telemetry status`. | Lockfile current (36 packages); pytest 24/24 passed; ruff and strict mypy clean (24 files / 18 source files); ESLint clean; web contract tests 3/3 passed; Next build compiled; `typecheck:web` passed after deleting `.next`, proving clean-checkout safety; telemetry reports `Status: Disabled`. | ADR-0006 | Closes the Phase A/B review blockers. Also records the two `numbers.v2.json` content deltas documented below. Phase C (PostgreSQL) not started. |
| E-013 | 2026-08-03 | Implemented ADR-0006 Phase D under accepted ADR-0007: anonymous learner bootstrap, the closed evidence vocabulary `1.0.0` with discriminated Pydantic payloads (`domain/evidence.py`), the deterministic progress projection (`domain/progress.py`), compensating `evidence.retracted` events, the privileged `DELETE /internal/learners/{id}` erasure route, the narrow learner-loop API routes, and the Next.js Numbers runtime with explicit loading, empty, and failure states. Ran `alembic upgrade head` against a freshly dropped schema, `pytest`, `ruff check`, `ruff format --check`, `mypy`, `npm run verify`, and `npm run test:e2e` against the built app on the live FastAPI/PostgreSQL/Redis stack. | Migrations applied to an empty schema; pytest 102/102 passed; ruff and strict mypy clean (36 source files); `npm run verify` passed end to end; Playwright 7/7 passed. The E2E suite found a real defect the unit tests could not: committing the unit of work in FastAPI's `yield`-dependency teardown released the response before the transaction committed, so a browser that immediately used a freshly minted learner id was told that learner did not exist. Writes now commit inside the handler. | ADR-0006, ADR-0007 | Supports that the Numbers learner loop persists and replays correctly. Does **not** evidence learning effectiveness: the reported rubric is the Learning Object's own keyword match, not a measure of understanding. Sessions are never ended (`ended_at` unused) and no retention window is set (ADR-0007 D6 leaves this open). Phase E (tutoring) not started; the E2E suite is not yet wired into CI. |
| E-014 | 2026-08-04 | Implemented ADR-0006 Phase E: a provider-neutral tutoring abstraction (`domain/tutoring.py` contracts, `providers/base.py` Protocol, `services/tutoring.py` router, `api/v1/routes/tutoring.py`) with one shipped provider, `deterministic-los`, that answers five task types by explicit rules over LOS v2 content and the accepted ADR-0007 progress projection; plus the "Ask for help" panel in `apps/web`. Ran `alembic upgrade head`, `pytest`, `ruff check`, `ruff format --check`, `mypy`, `npm run verify`, and `npm run test:e2e` against the built app on the live FastAPI/PostgreSQL/Redis stack. | Migrations applied; pytest 148/148 passed (46 new, covering task validation, provider routing and refusals, byte-identical repeat responses, citation grounding, out-of-scope refusal, degraded-database and unknown-learner paths, and the privacy boundary); ruff and strict mypy clean (42 source files); `npm run verify` passed end to end (web tests 12/12); Playwright 14/14 passed, including the new help-flow spec, with Playwright managing the API and production web server under `ACADEMY_E2E_MANAGE_SERVERS=1`. | ADR-0006, ADR-0007, A-013 | Supports that help can be given from governed content alone, that it is reproducible, and that it writes nothing. Does **not** evidence teaching effectiveness, understanding, or adaptivity. The provider is not an LLM and makes no adaptive-mastery claim; its only use of evidence is selecting the next unmatched rubric check, and the rubric remains a content-defined keyword match. Scope detection is vocabulary overlap with the lesson, so an on-topic question phrased in unfamiliar words can be wrongly refused, and an off-topic question that reuses lesson words can be wrongly accepted. Tutoring records nothing at all (A-013 open). The new `e2e` CI job was exercised locally through the same code path but has **not** been observed on a hosted runner; `astral-sh/setup-uv@v5` remains unpinned. Sessions are still never ended and no retention window is set. Phase F remains partly open. |
| E-015 | 2026-08-04 | Completed ADR-0006 Phase F locally: SHA-pinned every GitHub Action to an immutable commit (with the release recorded in a trailing comment), made the `e2e` CI job create and destroy its own PostgreSQL and Redis, uploaded Playwright traces/screenshots/videos and the captured API and web server logs on failure only, mirrored the advisory `npm audit` into the job summary, added `apps/web/tests/repository-hygiene.test.mjs` to guard the pinning and the absence of committed secrets, and added Playwright coverage for keyboard-only completion, axe-core WCAG 2.1 A/AA scans, browser privacy, visible loading, and recovery after a failed write. Then ran the whole suite from a destroyed state: `docker compose down -v`, deleted `.next`, `services/api/.venv`, `node_modules`, `e2e-logs`, and `test-results`; `docker compose up -d --wait`; `uv lock --check`; `uv sync --all-groups --locked`; `alembic upgrade head`; `alembic check`; `alembic downgrade base`; `alembic upgrade head`; `npm ci`; `npm run verify`; `npm run test:e2e` under `ACADEMY_E2E_MANAGE_SERVERS=1`; then inspected the captured server logs and the released ports. | Lockfile current (44 packages); migrations applied to an empty schema, `alembic check` reported no drift, and downgrade/upgrade round-tripped; `npm ci` reported 0 vulnerabilities; ruff and strict mypy clean (42 source files); pytest 149/149 passed; web unit tests 17/17 passed; Next build compiled and types generated; Playwright 28/28 passed with Playwright owning the API and the production web server; both server ports were released afterwards; `git status` clean. Zero unpinned actions remain. The four axe scans reported zero violations, each asserting a non-empty `passes` list so a scan that matched nothing cannot read as green. **The clean-state run found a real production defect:** the service never configured Python logging, and uvicorn configures only its own `uvicorn.*` loggers, so every `academy_api` record fell through to Python's handler of last resort and everything below WARNING — including the structured `TUTOR` line added in Phase E — was discarded in production while passing in tests, which install their own handler. Fixed in the app factory (`ACADEMY_LOG_LEVEL`, default `INFO`); the captured log then contained 11 formatted `TUTOR task=…` lines. A grep of both captured server logs for learner-authored text and lesson content returned 0 matches. | ADR-0006, ADR-0007, A-013 | Supports that the Numbers full-stack slice is reproducible from what is committed: no `.env` file exists in the repository, every setting defaults to the local Compose stack, and no test reaches a host outside `127.0.0.1` (asserted in `e2e/privacy.spec.ts`). Supports that only an opaque server-generated learner UUID reaches browser storage, that no tutor text is retained across a reload, and that the privileged erasure route is absent from the OpenAPI document and returns 404 when unconfigured. Does **not** evidence teaching effectiveness, understanding, or curriculum completeness — this is one Learning Object. **The hosted GitHub Actions workflow has never been observed running**; all four jobs were exercised locally through the same commands, which is not the same thing, and only a push or pull request can settle it. Action majors are pinned but deliberately not upgraded (checkout/setup-node/upload-artifact v4, setup-uv v5) until one hosted run is observed. Anonymous learner UUIDs do appear in uvicorn access-log paths, which is intended for an anonymous identifier but is still a linkable value in operational logs. Automated accessibility checking covers only machine-detectable failures; it is not an accessibility audit. Sessions are still never ended, no retention window is set, tutoring records nothing (A-013 open), and the TypeScript contract types remain hand-mirrored with no generator. |

Evidence entries must state the method, result, date, and related assumption or ADR.

## Owner decisions recorded with E-012

Recorded here rather than in a new ADR because none of these changes an accepted ADR
decision; each confirms or applies ADR-0006 as already written.

| Date | Decision | Rationale |
| --- | --- | --- |
| 2026-08-02 | The Phase A → Phase B ordering is accepted retrospectively as covered by ADR-0006. | Phase B work began before Phase A's `npm run verify` gate was formally run. Both phases now pass that gate together, and the review found no defect attributable to the ordering. Accepted as-is; the gate stands unchanged for Phases C–F. |
| 2026-08-02 | The preserved prototype step labels are the canonical production labels. | The prototype's `labels` map is existing governed copy validated under E-011. Reusing it verbatim avoids inventing new taxonomy copy without an ADR. `STEP_KIND_LABELS` in the API is now the single source of truth. |
| 2026-08-02 | Lesson `headings` are **not** promoted into the canonical contract yet. | The prototype also carries a per-kind `headings` map. Promoting it is not required by the current acceptance criteria, so it stays out of the contract. Tracked as A-011. |

## Content provenance — production seed

`packages/content/learning-objects/numbers.v2.json` is a copy of
`prototype/content/learning-objects/numbers.v2.json` (the object validated in E-011), with
exactly two intentional deltas. No other field was altered.

| # | Field | Prototype value | Production value | Rationale |
| --- | --- | --- | --- | --- |
| 1 | `nextConcept` | `"variables-algebra"` | `null` | ADR-0006 production scope is exactly one Learning Object. Retaining the pointer would create a dangling reference to a concept absent from `packages/content/knowledge-graph.json`. Restore when the next concept is promoted. |
| 2 | `provenance.source` | `"Sprint 1 Numbers vertical slice, derived under Academy Constitution SSOT v1.0. Migrated to LOS v2.0 under ADR-0003/0004/0005."` | `"Production seed for the ADR-0006 Numbers vertical slice, migrated from the Node prototype LOS v2.0 object."` | Provenance must name the artifact's actual origin and governing decision. Restated, not erased — the prototype lineage is retained in this table and in E-011. |

No pedagogical content (steps, experiments, mental models, analogies, rubric, measurement)
was changed. See E-012.
