{
  "id": "functions-graphs",
  "version": "2.0.0",
  "title": "Functions and Graphs",
  "scope": "Understand a function as a repeatable input-output rule and a graph as a visible record of that relationship.",
  "stability": "timeless",
  "beginnerEntry": "You can substitute values into a simple algebraic expression.",
  "nextConcept": "coordinates-vectors",
  "knowledge": {
    "conceptId": "functions-graphs",
    "prerequisites": [
      "variables-algebra"
    ],
    "relatedConcepts": [
      "input",
      "output",
      "mapping"
    ],
    "history": "Leibniz coined the word 'function' in the 1690s and Euler gave us f(x) in 1734 — notation for the idea that one quantity depends on another by a fixed rule. Every API, every spreadsheet formula, and every machine-learning model is still exactly this: input, rule, output.",
    "mentalModels": [
      {
        "name": "Machine with a slot",
        "description": "A function is a machine: put an input in the slot, the machine applies its one rule, an output comes out. Nothing else goes in; nothing else comes out."
      },
      {
        "name": "Faithful promise",
        "description": "A function promises that the same input always produces the same output. That determinism is what makes functions testable, graphable, and safe to build on."
      }
    ],
    "analogies": [
      {
        "analogy": "A vending machine: code B4 always drops the same snack",
        "strength": 5,
        "whenToUse": "When introducing input→rule→output to a complete beginner."
      },
      {
        "analogy": "A coffee grinder: beans in, grounds out — never the reverse",
        "strength": 3,
        "whenToUse": "When emphasizing the one-way direction of a mapping."
      }
    ]
  },
  "learning": {
    "objectives": [
      "Describe a function as an input-output rule.",
      "Evaluate a function for a given input.",
      "Read slope and intercept from a graph.",
      "Recognize functions in programs and prediction systems."
    ],
    "estimatedMinutes": 75,
    "steps": [
      {
        "kind": "observe",
        "prompt": "A machine receives 0, 1, 2, 3 and returns 1, 3, 5, 7. Describe the repeatable relationship without using formal notation first."
      },
      {
        "kind": "wonder",
        "prompt": "What must be true for an input-output process to be predictable enough to call it one rule?"
      },
      {
        "kind": "predict",
        "prompt": "For f(x) = 2x + 1, predict f(10), the input that produces 15, and how the plotted line changes if +1 becomes +5."
      },
      {
        "kind": "experiment",
        "experimentId": "function-machine-line",
        "prompt": "Play the function machine above: slide x across its whole range. What stays the same on every slide, and what changes? Where do you see the '+3' and the '×2' in the picture?"
      },
      {
        "kind": "fail",
        "prompt": "Break it: try to find one input x that produces two different outputs on different tries. You cannot — explain why that impossibility is the defining property of a function."
      },
      {
        "kind": "discover",
        "prompt": "State your discovery: the graph is not decoration; it is every input-output pair of the rule drawn simultaneously, which is why reading a graph is reading the function's full behavior."
      },
      {
        "kind": "explain",
        "prompt": "A function maps each allowed input to one output. A graph places input horizontally and output vertically, revealing change, intercepts, and patterns."
      },
      {
        "kind": "apply",
        "prompt": "Create a table for y = 3x - 2 at five x values and describe its graph. Then find one input-output rule in your own day, such as fare from distance or cooking time from weight, and name its input and output."
      },
      {
        "kind": "observe",
        "prompt": "A weather app turns any city into a temperature; a spam filter turns any email into a score. Both are functions with one output per input, even though no algebra formula is visible anywhere."
      },
      {
        "kind": "wonder",
        "prompt": "A prediction model is a function whose rule was learned rather than written by hand. If the rule is hidden inside the machine, how could you still study its behavior? Consider feeding it inputs and plotting the outputs."
      },
      {
        "kind": "predict",
        "prompt": "For f(x) = 2x + 1 and g(x) = -2x + 9, predict where the two graphed lines cross, what the crossing point means, and which function grows as x grows."
      },
      {
        "kind": "explain",
        "prompt": "Engineers probe functions as black boxes: choose inputs, record outputs, graph the pairs. The graph exposes the rule's behavior, with slope as sensitivity and intercept as baseline, even when the formula is unknown."
      },
      {
        "kind": "apply",
        "prompt": "Write pseudocode predict(x) returning 3x - 2, tabulate five (x, y) pairs, and read slope and intercept from your table. Notice the limitation: each row carries one input number, but a real customer or house is described by several numbers at once. That needs coordinates and vectors."
      }
    ],
    "experiments": [
      {
        "id": "function-machine-line",
        "type": "function-machine",
        "title": "Drive the function machine",
        "instructions": "Slide the input x and watch f(x) = 2x + 3 compute the output while the point rides along the line. The graph IS the machine's entire behavior, drawn at once.",
        "config": {
          "m": 2,
          "b": 3,
          "xMin": -5,
          "xMax": 5
        }
      }
    ]
  },
  "measurement": {
    "prePrompt": "Describe what you think a mathematical function does and what a graph can tell you.",
    "postPrompt": "Given g(x) = 4x + 3, calculate three points, explain slope and intercept, invert one output, and express the rule as code.",
    "successCriteria": [
      "Identifies input, rule, and output.",
      "Evaluates the rule correctly.",
      "Explains graph axes.",
      "Interprets slope and intercept in context.",
      "Expresses the same mapping in pseudocode."
    ],
    "masteryRubric": {
      "threshold": 3,
      "checks": [
        {
          "id": "input-output",
          "label": "Describe inputs and outputs explicitly.",
          "pattern": "(input|output|argument|return)"
        },
        {
          "id": "mapping",
          "label": "Describe the fixed rule or mapping.",
          "pattern": "(map|rule|machine|transform|assign|f\\(x\\))"
        },
        {
          "id": "graph",
          "label": "Connect the rule to its graph or slope.",
          "pattern": "(graph|plot|axis|curve|line|slope)"
        },
        {
          "id": "determinism",
          "label": "Note that the same input always gives the same output.",
          "pattern": "(same|every time|consistent|determin|predictable|always)"
        },
        {
          "id": "application",
          "label": "Connect functions to prediction, code, or models.",
          "pattern": "(model|predict|code|api|formula|use)"
        }
      ]
    }
  },
  "reasoning": {
    "misconceptions": [
      "A function is only a formula.",
      "One input may produce several outputs in the same function.",
      "A graph is merely decoration.",
      "Slope describes height rather than rate of change."
    ],
    "tutorGuidance": "Have the learner narrate one input traveling through the rule to one output, then connect table rows to plotted points and slope to output change per input step."
  },
  "provenance": {
    "source": "ADR-0002 staged curriculum derived from the Academy source vision. Migrated to LOS v2.0 under ADR-0003/0004/0005.",
    "status": "validated"
  }
}
